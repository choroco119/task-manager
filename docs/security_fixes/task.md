- [x] `app.js` にHTMLエスケープ用ヘルパー関数 `esc()` を実装する
- [x] 画面描画ロジック内の `innerHTML` 使用箇所を全て洗い出す
- [x] 各描画箇所で変数を `esc()` で囲むように修正する
    - [x] チームタブの描画 (`render`)
    - [x] グリッドヘッダー・サイドバーの描画 (`renderGrid`)
    - [x] 工程詳細パネルの描画 (`showPjDetail`)
    - [x] メンバー・チーム管理画面の描画 (`renderMemberBody`)
    - [x] 製番リストテーブルの描画 (`renderProjectBody`)
- [x] データのインポート・貼り付け時の整合性チェックを強化する
- [x] 修正後の動作確認（エスケープ処理の正常動作を確認）
