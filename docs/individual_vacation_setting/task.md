# タスク状況: 特別項目の管理機能追加

- [x] `app.js` のデータ構造に `specialItems` を追加、初期データをセット
- [x] `index.html` の管理モーダルに「特別項目」タブを追加
- [x] `app.js` の `switchMemberTab` と `renderMemberBody` に特別項目の管理UIを追加
- [x] 特別項目のCRUD関数群（追加、編集、削除、順序変更など）を `app.js` に実装
- [x] `index.html` の予定割り当てモーダルのバッジ部分を空のコンテナに変更
- [x] `app.js` の割当モーダル表示時に、バッジコンテナを動的に生成・描画する処理を実装
- [ ] 動作確認（管理画面でのCRUD操作、割当画面でのバッジ機能連携）
- [ ] 完了報告 (walkthrough.md 更新)
